{% set canon_text -%}
[Story Canon]
The following are rules that apply to the whole story:
{{ story.world_rules }}
{%- if summaries -%}[Previous Chapter Summaries]
{%- for summary in summaries -%}
Chapter {{summary.position}}:{{summary.title}} {{summary.summary}}
{%- endfor -%}
{%- endif -%}
[Chapter Canon]
The following are memories that apply to the chapter:
{{chapter.memory}}
[Chapter Goals]
You must aim to achieve the following within the chapter in the stated order:
{{chapter.goal}}
{%- if characters -%}
[Chapter Characters]
The following are characters that appear in the chapter along with their description.
Use these descriptions when generating responses
{%- endif -%}
{%- endset %}
[
  {
    "type": "text",
    "text": {{ canon_text | tojson }}
  }
  {% for char in characters -%}
  {%- set char_text -%}
Character: {{ char.name }}
{% if char.personality %}Personality: {{ char.personality }}{% endif %}
{% if char.motivation %}Motivation: {{ char.motivation }}{% endif %}
{% if char.note %}Story Traits: {{ char.note }}{% endif -%}
{%- endset -%}
,{
    "type": "text",
    "text": {{ char_text | trim | tojson }}
  }
  {%- if char.image_mime_type -%}
,{
    "type": "text",
    "text": "Use the following text to describe the physical appearance of {{char.name}}: {{ char.image_description | trim | tojson }}"
  }
  {%- endif -%}
  {%- endfor -%}
  {%- set instruction_text -%}
  All character and world information provided above must be treated as the source of truth.
  Maintain consistency in tone and character behaviour.
  [Prompt]
  {{prompt}}
  {%- endset -%}
,{
  "type": "text",
  "text": {{ instruction_text | tojson }}
}
]